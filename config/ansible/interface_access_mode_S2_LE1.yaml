---
- hosts: nd
  check_mode: false
  gather_facts: false
  tasks:
  - name: Configure S2_LE1.Eth1/2 as access interface
    cisco.dcnm.dcnm_interface:
      fabric: "{{ SITE2_FABRIC }}"
      state: merged
      config:
      - name: "{{ S2_LE1_INTERFACE_2 }}"
        type: eth
        switch:
        - "{{ S2_LE1_IP4 }}"
        deploy: true
        profile:
          admin_state: true
          mode: access
